console.log(":
    // Wersja 1 TO SAMO
    for (let i
    H
    0; i < 5; i++) {
    console.log("i=
    + i);
    }
    console.log("=
    // Wersja 2 TO SAMO
    for (let i
    =
    0; i < 5; i = i + 1) {
    console.log("i=
    + i);
    }
    console.log(":
    // Wersja 3 TO SAMO
    for (let i = 0; i < sth(); i
    console.log("i=
    }
    + i);
    H
    sth2(i)) {
    function sth(): number {
    }
    return 5;
    function sth2(i: number): number {
    }
    return i + 1;
    console.log("=
    // Wersja 4 TO SAMO
    let i = 0;
    for(; i < 5;) {
    console.log("i=
    i++; }
    console.log(":
    + i);
    // for (ZINCIJALIZUJ; BOOLEAN; ZWIĘKSZ/ZMNIEJSZ) { // }
    ")
    "')
    ")
    ")
    
    
    //Wersja 5 TO SAMO DALEJ, UWAGA MAMY 12, 13, 14 && sth3(), sth4(), sth5()
    let i2 =
    sth3();
    for(; sth4(12);) {
    console.log("i2=
    i2 =
    sth5(i2);
    }
    + i2);
    function sth3(): number {
    }
    return 0;
    function sth4(13: number): boolean { return 13 < 5;
    }
    function sth5(i4: number): number {
    return 14 + 1;
    }